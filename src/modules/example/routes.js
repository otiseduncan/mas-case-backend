import { authPlugin } from '../../middlewares/auth.js'
import { authRoutes } from '../auth/routes.js'

// Example Fastify route registration for ESM
export async function routes(fastify) {
  // ...route logic here...
}
