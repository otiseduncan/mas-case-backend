# === Server ===

PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# === Database ===
# For Railway public proxy
DATABASE_URL="postgresql://postgres:tWiWbsUTkIOvyfyehQuzInQSzQnhXCft@switchback.proxy.rlwy.net:15747/railway"

# === Auth ===
JWT_SECRET="super-secret-key"
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d

# === Seeding (optional) ===
# Provide a JSON array of demo users; if empty, defaults are created.
# Each item: { "email": "...", "password": "...", "role": "admin|manager|tech", "org": "MAS" }
DEMO_USERS_JSON='[]'
